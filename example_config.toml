# [global]
# progress_full_character = "="
# progress_empty_character = "="
# progress_prefix = "["
# progress_suffix = "]"
# time_format = "%Y-%m-%d %H:%M:%S %Z"

[banner]
color = "red"
command = "echo This is the legacy configuration format. See https://github.com/rust-motd/rust-motd/tree/main/docs/config_migration.md"

# [weather]
# url = "https://wttr.in/New+York,New+York?0"
# user_agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/110.0.0.0 Safari/537.36"
# proxy = "http://proxy:8080"

# [service_status]
# Accounts = "accounts-daemon"
# Cron = "cron"

# [docker]
# Local containers MUST start with a slash
# https://github.com/moby/moby/issues/6705
# "/nextcloud-nextcloud-1" = "Nextcloud"
# "/nextcloud-nextcloud-mariadb-1" = "Nextcloud Database"

# [uptime]
# prefix = "Up"

# [load_avg]
# format = "Load (1, 5, 15 min.): {one:.02}, {five:.02}, {fifteen:.02}"

# [user_service_status]
# gpg-agent = "gpg-agent"

# [ssl_certificates]
# sort_method = "manual"
#
#    [ssl_certificates.certs]
#    CertName1 = "/path/to/cert1.pem"
#    CertName2 = "/path/to/cert2.pem"

# [filesystems]
# root = "/"

# [memory]
# swap_pos = "beside" # or "below" or "none"

# [cg_stats]
# state_file = "cg_stats.toml"
# threshold = 0.01

# [fail_2_ban]
# jails = ["sshd", "anotherjail"]

# [last_login]
# sally = 2
# jimmy = 1

# [last_run]
